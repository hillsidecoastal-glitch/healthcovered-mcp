services:
  - type: web
    name: healthcovered-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: mcp run server.py --transport sse --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
